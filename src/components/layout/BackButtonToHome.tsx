/**
 * Keep browser history behavior completely standard.
 *
 * This component is intentionally a no-op so inner-page navigation follows the
 * same back/forward flow users expect on typical websites and SPAs.
 */
export function BackButtonToHome() {
  return null;
}
